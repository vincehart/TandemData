name: Post JSON File to Two URLs
on:
  push:
    branches:
      - main # Runs only when pushing to the main branch
  workflow_dispatch: # Allows manual trigger
jobs:
  post_json:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
      - name: Send JSON Data to First URL
        run: |
          curl -X POST "https://:PC3HD5CfTVuWb6WMmISHbA@eu.tandem.autodesk.com/api/v1/timeseries/models/urn:adsk.dtm:pdylyeOnQceomhTWt2tpVg/streams/AQAAAMdR73fZI0RzkVDVPxHP6G4AAAAA" \
            -H "Content-Type: application/json" \
            -d @stream.json
      - name: Send JSON Data to Second URL
        run: |
          curl -X POST "https://:mHIoe8coSJyNPNkAeNEDYA@eu.tandem.autodesk.com/api/v1/timeseries/models/urn:adsk.dtm:pdylyeOnQceomhTWt2tpVg/streams/AQAAAEhRyf8SmEJrjRFKPrP8UAYAAAAA" \
            -H "Content-Type: application/json" \
            -d @stream.json
